//! Rust translation of doomgeneric/r_local.h
//! Refresh (R_*) module, global header. All the rendering/drawing stuff.

use crate::r_bsp::*;
use crate::r_data::*;
use crate::r_draw::*;
use crate::r_main::*;
use crate::r_plane::*;
use crate::r_segs::*;
use crate::r_things::*;
use crate::tables::*;
