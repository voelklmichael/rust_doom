//! Joystick input (i_joystick.h, i_joystick.c)
//! Original: i_joystick.h, i_joystick.c

use std::sync::Arc;
use std::sync::Mutex;

pub struct I_JoystickState;

impl I_JoystickState {
    /// Original: void I_InitJoystick(void)
    pub fn i_init_joystick(&self) {
        todo!("Basic stage-0 stub")
    }
}
